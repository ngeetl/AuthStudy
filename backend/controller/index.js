const jwt = require("jsonwebtoken");

const signup = (req, res) => {
    // 클라이언트에서 유저 정보를 넘기면
    // accessToken을 발급해줌
}

const signin = (req, res) => {
    
}



module.exports = {
    signup,
    signin,
};